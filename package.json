{
  "name": "operation-falcon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An original run-and-gun action platformer homage, built on raw Canvas 2D.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node tests/run.mjs && node tests/movement.mjs && node tests/shooting.mjs && node tests/enemies.mjs",
    "screenshots": "node tests/screenshot.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
